<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.zerobugz.drdasalem">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/QIBusSoftui"
        android:usesCleartextTraffic="true"
        tools:ignore="GoogleAppIndexingWarning"
        tools:targetApi="q">
        <activity android:name=".activities.DistrictSchemeWiseBeneficiaryList" />
        <activity android:name=".activities.DistrictPanchayatSummary" />
        <activity android:name=".activities.AddWorkStage" />
        <activity android:name=".activities.WorkStageList" />
        <activity android:name=".activities.AddWorkOrder" />
        <activity android:name=".activities.WorkOrderList" />
        <activity android:name=".activities.SchemeList" />
        <activity android:name=".activities.PaymentBeneficiaryList" />
        <activity android:name=".activities.AddBilling" />
        <activity
            android:name=".activities.AddBeneficiary"
            android:theme="@style/FullScreen" />
        <activity
            android:name=".activities.VerifyActivity"
            android:theme="@style/FullScreen" />
        <activity
            android:name=".activities.LoginActivity"
            android:theme="@style/FullScreen" />
        <activity
            android:name=".activities.SplashActivity"
            android:theme="@style/FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.MainActivity" />
        <activity android:name=".activities.SchemeWiseBeneficiaryList" />
        <activity android:name=".activities.StageWiseBeneficiaryList" />
        <activity android:name=".activities.PanchayatSummary" />
        <activity android:name=".activities.BlockSummary" />
    </application>

</manifest>